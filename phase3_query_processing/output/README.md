# Phase 3 — Output

This folder holds **sample outputs** of the Query Processing phase so you can see how Phase 3 behaves.

- **`sample_query_processing_results.json`** — Example queries and the Phase 3 result for each (enriched_query, fund_resolved, fund_key, canonical_name, url, section_filter, clarification_message).

Regenerate with:
```bash
python -m phase3_query_processing.write_sample_outputs
```
